spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:mem:syncnotes;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH}
    username: ${SPRING_DATASOURCE_USERNAME:sa}
    password: ${SPRING_DATASOURCE_PASSWORD:}

logging:
  level:
    com.syncnotes: DEBUG
